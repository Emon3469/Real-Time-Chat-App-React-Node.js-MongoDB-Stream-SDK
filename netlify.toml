[build]
  base    = "frontend/chat_app_fronend"
  command = "npm install && npm run build"
  publish = "dist"

# SPA fallback — send all routes to index.html so React Router handles them
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
